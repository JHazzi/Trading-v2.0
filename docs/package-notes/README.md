# Package Notes

Future patch/ZIP installation notes belong here instead of the repository root.

These files are noncanonical and may be moved to `docs/archive/package-notes/` after the corresponding package is superseded.

For current project state, use `docs/INDEX.md`.
